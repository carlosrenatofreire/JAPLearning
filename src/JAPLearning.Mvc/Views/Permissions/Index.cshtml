@model List<JAPLearning.Mvc.ViewModels.Parameters.PermissionViewModel>
@{
    ViewData["Title"]      = "Permissões";
    ViewData["ActiveMenu"] = "permissions";
    Layout = "~/Views/Shared/_LayoutApp.cshtml";
}
@await Html.PartialAsync("_CrudStyles")

@if (TempData["Success"] != null)
{
    <div class="alert-success-md"><i class="bi bi-check-circle-fill"></i> @TempData["Success"]</div>
}

<div class="page-header">
    <div>
        <h4 class="page-header-title">Permissões</h4>
        <p class="page-header-sub">Gestão das permissões por módulo</p>
    </div>
    <a asp-action="Create" class="btn-md-primary"><i class="bi bi-plus-lg"></i> Nova Permissão</a>
</div>

<div class="md-card" style="padding:0; overflow:hidden;">
    <table class="crud-table">
        <thead>
            <tr>
                <th>Nome</th>
                <th style="width:180px;">Módulo</th>
                <th>Descrição</th>
                <th style="width:100px; text-align:center;">Estado</th>
                <th style="width:100px; text-align:center;">Acções</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model.OrderBy(p => p.ModuleName).ThenBy(p => p.Name))
            {
                <tr>
                    <td style="font-weight:600; color:var(--md-text-primary);">
                        <i class="bi bi-key" style="color:var(--md-accent); margin-right:8px;"></i>
                        @item.Name
                    </td>
                    <td>
                        <span class="md-badge md-badge-secondary">@item.ModuleName</span>
                    </td>
                    <td style="color:var(--md-text-muted); font-size:13px;">
                        @(string.IsNullOrWhiteSpace(item.Description) ? "—" : item.Description)
                    </td>
                    <td style="text-align:center;">
                        @if (item.IsActived)
                        {
                            <span class="badge-active"><i class="bi bi-circle-fill" style="font-size:7px;"></i> Activo</span>
                        }
                        else
                        {
                            <span class="badge-inactive"><i class="bi bi-circle-fill" style="font-size:7px;"></i> Inactivo</span>
                        }
                    </td>
                    <td style="text-align:center;">
                        <div style="display:flex; gap:6px; justify-content:center;">
                            <a asp-action="Edit" asp-route-id="@item.Id" class="action-btn" title="Editar">
                                <i class="bi bi-pencil"></i>
                            </a>
                            <form asp-action="Delete" asp-route-id="@item.Id" method="post" style="margin:0;"
                                  onsubmit="return confirm('Eliminar a permissão \'@item.Name\'?');">
                                @Html.AntiForgeryToken()
                                <button type="submit" class="action-btn danger" title="Eliminar">
                                    <i class="bi bi-trash"></i>
                                </button>
                            </form>
                        </div>
                    </td>
                </tr>
            }
            @if (!Model.Any())
            {
                <tr>
                    <td colspan="5" style="text-align:center; padding:40px; color:var(--md-text-muted);">
                        <i class="bi bi-key" style="font-size:32px; display:block; margin-bottom:8px;"></i>
                        Nenhuma permissão encontrada.
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>
